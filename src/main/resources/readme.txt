The keystore file jetty.jks and the key in it was generated using the following command:

keytool -keystore jetty.jks -alias jetty -genkey -keyalg RSA -validity 365

The password for both the keystore and the key is "password"
